- hosts: ansible
  connection: local
  roles:
      - aws-keys
        #- aws-sg 
- hosts: app 
  roles:
      #- base 
      #- nginx
      #- passenger
      #- rails
      #- codepicnic-app
  remote_user: ubuntu
  become: yes
  become_user: root
  become_method: sudo
- hosts: db 
  roles:
      #- base 
      #- redis 
      #- mysql
      #- codepicnic-db 
  remote_user: ubuntu
  become: yes
  become_user: root
  become_method: sudo
